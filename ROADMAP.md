# Temper — Roadmap

Semi-stable. Update the **Status** column as milestones complete; anything structural
belongs in `ARCHITECTURE.md` §9 instead. Sessions: your milestone's brief in
`docs/briefs/` overrides the one-line summary here, and the constitution overrides both.

**Standing priority note:** Anvil, FrontierView interview prep, and DepthCharge bench work
rank ahead of Temper in the owner's queue. Temper sessions are opportunistic; keep every
milestone evening-sized on the reference box (constitution §6.9).

All milestones are agentic (Opus / Claude Code sessions converging on pytest red/green);
there is no bench track. M0's golden export was the one owner-input step; re-exports are
`make goldens` against a FrontierView checkout (see `docs/vendor/frontierview-goldens.md`).

## Milestones

| #  | Milestone                    | Goal / definition of done                                                                                           | Depends on | Status |
| -- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------- | ------ |
| M0 | Oracle + goldens             | Repo scaffold (pytest, Makefile, pinned requirements); `oracle/` closed forms (κ, sinh trajectory, E/V, frontier, TWAP moments); FrontierView goldens vendored with provenance; oracle matches every golden case within pre-stated tolerance; green from a clean clone. | —          | ☑ **Done** 2026-08-04 — brief: `docs/briefs/M0-oracle-and-goldens.md`; 16 vendored cases + 17 frontier points matched to ~1e-15, ten orders inside the 1e-6 tolerance. Yielded three §9 amendments. |
| M1 | Env + analytic differential  | `ExecutionEnv` (Phase-1 dynamics, §4 contract *as amended* — see `ARCHITECTURE.md` §4's *Amended by M0* and *Amended by M1a* bullets, which name their §9 entries by title); TWAP + AC schedule wrapped as policies; Monte-Carlo harness; simulated cost mean/variance for both fixed policies match closed forms within pre-stated CI. **The load-bearing correctness milestone** — env bugs die here, not in training curves. | M0 | ☑ **Done** 2026-08-04 — briefs: `docs/briefs/M1-env-and-analytic-differential.md`, then `docs/briefs/M1a-acceptance-hardening.md`. Deep tier (27 cells × 200 k episodes) green in 5 min 17 s, no cell using more than 55 % of its band, and 70,200,000 calls into the real `step` loop *asserted* rather than assumed. The differential is now only statistical about the *draws*: an exact per-episode identity pins the realised noise to the specific shocks the env made (worst 1.3e-13), so the cost assembly holds by construction. Seven exact identities, a variational certificate, and an observation-minimality guard that closes M2's leak before M2 exists. Yielded two §9 amendments (Phase 1 is linearised end-to-end with `cost_moments` quarantined; the shock lands before the bin, which fixes the variance sum's index range). |
| M2 | PPO rediscovery              | Single-file PPO (control-task smoke test green first); trained on `(t, inventory)` at one λ; mean shortfall within brief-stated ε of the oracle across ≥5 seeds; trajectory-overlay figure (agent vs sinh) committed to `results/`. | M1 | ☑ **Done** 2026-08-06 — brief: `docs/briefs/M2-ppo-rediscovery.md`. λ = 10^−3.5 fixed by task 0's oracle-only rule *before* any training code (TWAP gap 56.30 %, largest optimal bin 32.6 % of X), and re-derived on every run so the config cannot drift from it. **The finding is the estimator, not the agent.** On the noise-free reward the agent rediscovers the sinh to median +0.0115 % of J_optimal — 0.0002 of the TWAP gap against an ε of 0.05, worst seed 0.0003 — with a median ‖δ‖₂ of 1 336 shares against a *derived* allowance of 28 797. On sampled rewards, same hyperparameters and same seeds, it misses both criteria (median 0.098, worst 0.819) and the failure is a lottery rather than a plateau: one seed lands inside ε, another barely learns. Both sweeps are committed and differ in one config field. Graded analytically through `schedule_moments` — one deterministic rollout per seed, zero Monte-Carlo error — behind a bitwise eval-determinism assertion and a hard red-flag test, both green on every seed. Yielded four §9 amendments. |
| M3 | Frontier sweep               | λ grid; RL-traced (E, V) points overlaid on the analytic efficient frontier — the hero figure; per-λ tolerance table committed. **Note (M1a):** `V` floors at `σ_bin²X²`, so the frontier approaches that floor as λ → ∞, not the origin — and every schedule shares the floor (≈20 % of TWAP's `V` at `N = 13`, more for front-loaded schedules). State the per-λ tolerance table on **excess over the floor**, not on total `V`, or it goes systematically insensitive at exactly the high-λ end where the schedules separate most. **Note (M2), pre-stated so M3 inherits a decision rather than making one under pressure:** (a) *artefact size* — M2 commits per-update training traces **whole** (1.2 MB per 5-seed sweep) because they are what make the seed spread checkable rather than asserted; 17 λ multiplies that to ~20 MB per estimator, so M3 **must** set `results.trace_points: 128` (uniform subsample keeping both ends; the mechanism is `temper.eval.sweep.thin`, already committed and tested). Per-seed *graded* values and trajectories stay whole at every λ — they are what dispersion is computed from and cost ~13 floats each — and full-resolution traces are kept for the one λ M2 already reports, so the M2↔M3 comparison stays exact. (b) *estimator* — M3 inherits the control variate (§9 *Phase-1 rediscovery trains on the noise-free reward…*); a λ-sweep on sampled rewards would trace a frontier whose scatter is dominated by which seeds won their lottery. Report the sampled-reward control at M2's λ as the reference point, not at all 17. | M2 | ☑ **Done** 2026-08-18 — brief: `docs/briefs/M3-antithetic-and-frontier.md`. **Antithetic pairing replaces the control variate, and is validated before it is used.** Ten seeds at M2's λ: median gap fraction 0.000168 against a gate of 0.002 stated on M2's committed CV result (0.000204), worst seed 0.001451, no red flags — with the cancellation *measured* inside the run (per-update reward variance 3,377 bps² sampled vs 3.4e−08 averaged, ratio 1.0e−11) rather than inferred from the outcome. On the five seeds shared with M2 the two regimes agree **bitwise**, objective and trajectory, because their rewards differ below float32 resolution at the agent boundary — and all ten seeds reproduced bitwise across a re-run at a different revision. The frontier is 9 λ × 10 seeds at 751 updates (amendment 1: a re-anneal, evidence-based, recorded before the sweep), 19.65 h, **ε met at 8 of 9 and the red-flag test green on every seed at every λ**. The one miss (10^−5) is the tolerance's denominator, not the agent: absolute excess stays between +0.004 % and +0.33 % across four decades while the gap fraction moves three and a half orders of magnitude. Hero figure `results/m3_frontier.png` on excess-over-floor axes, every seed drawn. Yielded three §9 amendments, two house notes, and one provenance defect fixed (`git_dirty` could fire on a clean tree). |
| M4a | Broken assumptions — the power law | The env's temporary impact becomes FrontierView's **calibrated 0.6-power law** (the vendored mis-specification, not an invented one) behind the same env interface, injected rather than subclassed; the power-law world's own optimum solved and **certified** as first-class oracle surface; the metric registries keyed by world so a metric grades the world that charges it; the differential re-run at M1's tiers against the new env (invariant 6); the four guarantees the world inherits checked *before* training; Phase 1 reproducing bitwise through the seam; AC-schedule degradation quantified; ten seeds at the rule-selected λ graded against the certified optimum, with the capture fraction and the absolute excess in bps reported together. | M2 | ☑ **Done** 2026-08-19 — brief: `docs/briefs/M4a-power-law-world.md`. **The project's first earned advantage, and it is bounded on both sides.** Task 0's three gates green on the reference box, every pre-stated number reproduced: both encodings' rules select 10^−3.5, the available advantage `J_opt(tangent) − J_pow*` is 0.03674 bps (1.542 % of the objective, 2.50 % of expected cost), and the median tolerance's band of 4 739 shares sits inside a 16 878-share separation — 3.56×. **The reference is solved and certified**, not trusted: Cholesky PD, relative KKT residual 1.2e-15 against a 1e-12 bar, 3 600 perturbations uphill, an independent bisection solver agreeing to 3.1e-15 of X, and the same machinery at exponent 1 returning `optimal_trajectory` to 3.5e-16 — no scipy. Ten seeds captured a median **99.4 %** of the advantage (IQR 0.2 %, worst seed 99.0 %), a median absolute excess of **+0.00021 bps**, median ‖δ‖₂ 727 shares against a derived *local* band of 4 739 — 23× closer to the optimum than the closed form is — with the red-flag test green on every seed. **The tolerance's denominator moved and that is the milestone's methodological finding**: 5 % of this λ's TWAP gap is 1.8–2.0× the entire available advantage, so M3's ε could not have distinguished success from complete failure. Phase 1 reproduces *bitwise* through the new env seam. Task 4's four inherited guarantees green — and they caught the antithetic mirror charging the wrong world before training started. Yielded three §9 amendments and one two-hour lesson: `build_document` was only reachable behind a training loop, so a dropped line discarded ten trained seeds; `tests/test_sweep_document.py` now exercises it in milliseconds. |
| M4b | Broken assumptions — stochastic liquidity | Liquidity becomes a second, *independent* noise source, which enriches the observation and therefore breaks analytic grading, the antithetic pair's action-identity check and the "no Monte-Carlo interval" property all at once. Split from M4a for that reason: bundled, a red result could not be attributed to a break. Sampling intervals arrive here. | M4a | ☑ **Done** 2026-08-23 — brief: `docs/briefs/M4b-stochastic-liquidity.md`. **The claim, in full: with a ONE-PARAMETER INVENTED liquidity process (`sigma_L` = 0.5), seeing liquidity is worth 2.6% of the objective and ten seeds captured a median 99.0% of it.** FrontierView has no liquidity process, so §7's vendored-not-invented cover does not reach this milestone and every number below is about Temper's own model. **Task 0 regenerated all of the brief's predictions on the reference box and they held** — the brief was written in a cloud container on unpinned numpy and none of it was an artefact; measured `J_static*` 2.49661 bps, `J_DP` 2.43449, adaptive advantage 0.06212 (2.55%), level shift 0.00234 (3.8% of the advantage), clairvoyant bracket 9.8% — all four gates green. **The denominator is the *adaptive* advantage `J_static* − J_DP`**, not `J_M4a − J_DP`: the difference is a level shift any static solver picks up for free by re-solving at the coefficient `A·E[L^−0.6]`, both rungs are closed forms because differencing two simulated levels would put a 0.002 bps quantity under a per-path SD of 0.18, and it is reported on its own line everywhere. **Gate 1 needed a decision the brief asked to be *recorded*.** Liquidity is not a new cost encoding — the charge is unchanged and what is random is the market — so the selection rule is applied to a third *reading*, and the two candidates disagree: the static reading selects 10^−3.5 (agreeing with M3 and M4a), the adaptive one selects 10^−4.0 by clearing the 20 % bar there by **0.011 percentage points**. The static reading was taken and the rejected one is recorded with its margin, because both of the rule's conditions are properties of a *schedule* and a policy has no single schedule. **Three of the four things the brief predicted would break did not.** The antithetic pair holds liquidity *common* and negates only the price, so both halves see the same observation, take the same action, and the price noise still cancels **exactly** (3.4e-16 bps per step) — §9's *a second, independent noise source* is half right, and what actually ends action identity is an observation the halves *disagree* about. M1a's noise identity survives (2.4e-14 relative). What does end is analytic grading: a liquidity-observing schedule is closed-loop, so the grade becomes `E[cost \| L]` — exact given the path, **no price sampling anywhere** — averaged over 20,000 held-out paths, paired against the static optimum's closed form for a half-width of 1.40% of the effect. **The reference is converged and bracketed, not certified**, and says so in the file: M4a earned that word with a Cholesky factorisation, and a stochastic DP has no such object. At `sigma_L` = 0 the DP returns M4a's certified value to 1.8e-06 bps, which is what ties the new machinery to a number that was certified. The red-flag test came out *sharper* than predicted: perfect information beats any policy on **every path**, so it is a count with no interval in it: zero paths below the relaxation across all 10 seeds. **The control is part of the claim.** Re-graded with the observed liquidity drawn independently of the liquidity charged, the same policies capture a median -1.01 against a pre-stated 0.15 bar, so the gap between 0.99 and -1.01 is what says the agent uses the signal rather than having found a better fixed schedule. The agent trained through reward noise the pair cannot remove — 3.269e-02 bps² per update against M4a's exactly zero — which is the milestone rather than a problem with it. σ_L is invented, so the result is a curve: the advantage is 0.01474 bps at 0.25, 0.06212 bps at 0.5, 0.15223 bps at 0.75. Yielded five §9 amendments (four predicted, one not: the λ reading). Figure: `results/m4b_adaptivity.png`; artefacts `results/m4b_reference.json` and `results/m4b_liquidity.json`. |
| M5 | Alpha-aware execution        | Weak short-horizon signal in the observation; agent learns to tilt the schedule; advantage holds on held-out seeds; overfit check (signal-shuffled control) green. | M4b | ☐ |
| M6 | Anvil live leg *(stretch)*   | `client/` works a parent order on the live Anvil book via `PROTOCOL.md` (vendored snapshot) — third independent client; arrival-slippage measured and reported as a demo, not an evaluation (constitution §7); DepthCharge panel cameo optional but delightful. | M2 + Anvil deployed | ☑ **Done** 2026-08-22 — brief: `docs/briefs/M6-anvil-live-leg.md`. **The number was predicted before it was measured, and that is the milestone.** The trained policy — M4a's median seed by a committed rank rule, exported as a `.npz` and run through a plain-numpy forward pass that reproduces the training-time actions to 3e-8 with no torch on the client's import path — worked a 1,000-share sell over 13 bins on a live Anvil book as `PROTOCOL.md`'s third independent client, **zero upstream changes**. A committed counterparty ladder plus a deterministic policy plus deterministic matching makes every fill computable in closed form beforehand, and all three shapes came back matching level for level: reference **11.21 bps**, thin **22.85**, wide **33.63**, each to the digit and each reproduced across two revisions. A dozen lines of Python against Anvil's C++ matcher over a wire — M1's differential applied to the wire leg, and the only thing arrival slippage from a self-built book *can* certify. The thin ladder is where the closed loop is exercised rather than merely present: bin one asked 421 against 375 of depth, swept all eight levels, was filled short, had its remainder cancelled (**accepted is not filled** — Anvil has no market orders), and carried the shortfall into the next observation, a state `ExecutionEnv` has never produced. The feeder run is reported separately: 641 third-party fills during it, the limit moving 675 ticks between bins, still filled and reconciled at 12.36 bps — and with the seed pinned it gave 12.3820 / 12.3752 / 12.3752 / 12.3587 across four attempts, which is *weak reproducibility* measured rather than asserted. Every run reconciles by `takerId` or is void with no rescue path; deleting one `trade` frame from a live run — the loss the wire cannot signal — voids it, shown end to end. **The optional deployment run was taken** (2026-08-23) against `anvil.garethcooke.com` over TLS on its 12-ticker roster: 1,000 of 1,000 filled, 1,000 attributed, and **void** on 236 third-party fills, which is precisely what the brief predicted for a shared unauthenticated floor. Reaching it needed `wss://` support the client never had and exposed three defects that only a public venue makes serious — a `build: false` teardown that swept the wrong list and could strand a live sell nobody could cancel, maker-side fills discarded so a rested remainder would have been sold twice, and a teardown iterating the list its own cancels append to. Yielded one §9 amendment (the committed policy artefact) and two brief amendments. |
| MP | Portfolio portal             | **Executes in the `garethcooke-portfolio` repo.** Stage 1 (after M3): `/projects/temper` live with the hero figure, In Progress badge, tags, repo/architecture links. Stage 2 (after M6): live-leg writeup. Stage 3 (after M4b): hosted `/temper/architecture` write-up page and its `writeup-sources.json` entry. | Stage 1: M3 · Stage 2: M6 · Stage 3: M4b | ☑ **Stages 1–2 done** 2026-08-23 — brief: `garethcooke-portfolio/docs/briefs/MP-temper-portal.md` (it executes there, so it lives there). **Stage 1** (2026-08-20): `/projects/temper` live. The hero is `results/m4a_degradation.png`, not the frontier: the frontier shows the agent *agreeing* with a closed form, while the degradation figure shows the closed form being wrong across four decades of λ and the agent finding the truth anyway — the more interesting claim to a reader who has fitted an Almgren–Chriss model. `results/m3_frontier.png` carries second, as the Phase-1 evidence the advantage claim rests on. The entry sits after DepthCharge so the trading cluster reads as one body of work, and `GalleryVariant` gained `"Figures"` because calling a plotted frontier a screenshot on a page whose argument is careful labelling would not do. The copy states each milestone's claim with its denominator and says plainly that there are no real fills or historical data anywhere in the project. **Stage 2 done** (2026-08-23, `1daf6bc`): the live leg is on the page, and the void is the argument rather than a caveat — the seventh `keyDecisions` entry is titled *"Your live run reports a number and then says it's void. Which is it?"* Four edits to `src/data/projects.ts`: a five-paragraph Phase-3 block after the test-suite paragraph, a replaced closing sentence that reaches both the `<meta>` description and the `/projects` card, `"WebSocket"` placed where Anvil and DepthCharge place their protocol tag, and that entry. Plus one deletion the brief had not scoped and the page needed: ¶8 still called the live leg a stretch goal that *would be* plumbing evidence, two paragraphs above the sentence saying it ran on a wire. Markdown is stripped as in Stage 1, with a second reason for the backticks — `overview` is a template literal, so a literal backtick ends the string mid-paragraph. Build and lint clean, 19 static pages with `temper.html` prerendered, both themes read on the production build, zero markdown artefacts in `innerText`, and the new card at 15.79:1 / 6.70:1. **Stage 3** — a hosted `/temper/architecture` write-up and its `writeup-sources.json` entry — is deliberately later, gated on M4b so the constitution has stopped moving before a second copy of it goes on the web; `githubUrl` + `docsUrl` satisfy the row's "repo/architecture links" until then. |

## Backlog (not scheduled)

- **Where the noise threshold actually is** (raised by M2, 2026-08-05). M2 measured the two
  ends and nothing between: on sampled rewards PPO plateaus around a sixth of the TWAP gap,
  and with the variance driven to *exactly* zero it lands on the optimum. The interesting
  curve is in between — subtract a fraction `c` of M1a's noise term rather than all of it,
  sweep `c` from 0 to 1, and read off the noise level at which rediscovery becomes feasible
  inside the budget. That is the honest quantitative version of "RL under noise recovers
  AC", it costs one extra config field, and it is the number M4's harder environments will
  want. Deliberately *not* done in M2: the brief's task 3 sanctions the variate or nothing,
  and a `c`-sweep is a study rather than a fallback.
- **A `Market`-style derived-quantities object — Temper-side: nothing to do** (raised by the
  2026-08-04 audit, re-scoped and checked 2026-08-20). Ordinary internal tidiness, true
  whether or not the oracle shares code with anything — which is why it is no longer filed
  under the code-sharing question, now closed in `ARCHITECTURE.md` §9 (*The oracle stays
  independent, and the question is closed rather than carried*). Recorded rather than dropped
  because a re-scope nobody writes down gets re-raised: `temper/oracle/model.py`'s `Market`
  has owned `dt`, `v_hourly`, `sigma_bin` and `times` since M0 — "so that no caller
  re-derives them and drifts" — and nothing in `temper/` re-derives any of them, the only
  bare `6.5` in the package being the `TRADING_HOURS_PER_DAY` definition itself. The
  three-plus unnamed-literal call sites the audit counted are *FrontierView's*, which is why
  the cross-referenced item below has no Temper-side counterpart. The one place it bites
  Temper is `tools/export_frontierview_goldens.py`, which re-derives `v_hourly`, `dt` and
  `sigma_bin` from FrontierView's constants — forced, not debt: the script imports
  FrontierView's modules and the standard library only (§7), so it cannot reach `Market` and
  must not. The `sigma_bin` re-derivations in `tests/` are the same thing deliberately: an
  independent second derivation is what makes a differential test a test.
- Transient impact with exponential decay (Obizhaeva–Wang-style) as a further Phase-2
  break beyond M4 — demoted from M4 when the calibrated power law was promoted to lead.
- **The power-law frontier** (raised by M4a, 2026-08-19). M3 swept nine λ in the linearised
  world; M4a trains one point in the power-law world. Re-running the sweep there would show
  how the available advantage moves along the frontier — the oracle half is already drawn in
  `results/m4a_degradation.png` and peaks at 2.33 % near 10^−3, away from the λ the rule
  selects — but the agent half is nine more training points and a sized milestone of its
  own, not a stretch goal.
- **Price each bin at the level it intends to consume, not through the whole book**
  (raised by the M6 merge, 2026-08-23 — from the abandoned first attempt, which reached
  the opposite conclusion before any code ran). `client/ladder.full_sweep_limit` prices
  through the far side of the observed book, which guarantees a bin is never short
  because it was priced timidly. The alternative is to price at exactly the worst level
  the bin *means* to reach, and the argument for it is detection-theoretic rather than
  economic: a deeper limit sweeps further than predicted and **hides** the discrepancy
  inside a plausible fill, while a limit at the intended worst level leaves a residual
  and makes the surprise loud. Both are defensible and the committed runs used the
  first, so this is not a defect — but it is the sharper instrument, and it was arrived
  at independently, which is the reason to keep it. **Not free:** it changes the
  realised fills, so it re-baselines all four `results/m6_anvil_*.json` and their
  predictions. A deliberate task with the re-run recorded, not something done in
  passing.
- SAC / behaviour-cloning-warm-start comparison against the PPO baseline.
- C++ inference leg: ONNX export of the trained policy + a Crucible post (policy inference
  under fire; ties the ML work back to the C++ story).
- Real-data calibration (LOBSTER or crypto L2) replacing the synthetic parameter set.
- Limit-order placement / queue-position action space — the v2-sized extension.
- **The figures are light-ground and the portal is dark** (raised by MP Stage 1, 2026-08-20). Both
  committed PNGs are RGBA with a fully opaque white background; against the portal's
  `--bg-card` (`#0c1220`) that is roughly an 18:1 luminance step, so each reads as a lit
  panel rather than a figure on the page. Legible, and the lightbox at full size is good,
  but the fix belongs in `temper/eval/figures.py` as a transparent or theme-neutral
  ground, not as CSS on the site. The cost is why it is not a tweak: restyling changes
  the bytes of every committed figure, which re-baselines the byte-identical redraw
  assertions M2, M3 and M4a all carry. A deliberate task with the re-baseline recorded,
  not something done in passing.
- FrontierView-side (lives on FrontierView's backlog, cross-referenced only): the
  discrete-κ convention question surfaced by M0 — the goldens pin `f87795f6`, so if
  upstream ever adopts `cosh(κτ) = 1 + μ/2`, that is a golden re-vendor with fresh
  provenance, not a Temper break; and a `Market`-style derived-quantities object **in
  FrontierView's compute core** (2026-08-04 audit: `v_hourly`, `dt`, `sigma_bin` re-derived
  at three-plus call sites, with unnamed literals) — Temper's own `Market` already does this,
  so the item above closes the Temper side rather than duplicating this one.
- Anvil-side (lives on Anvil's backlog, cross-referenced only): feeder realism — Hawkes
  arrivals / mirror mode — which would make the M6 book livelier; the sequenced L2 feed.
- DepthCharge cameo hardening: a dedicated capture of the agent working an order, vendored
  as a DepthCharge replay trace.
